{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('instructor') }}
{% endblock %}

{% block title %}Edit Instructor{% endblock %}

{% block body %}
    <div class="instructor-page edit-page">
    <h1>Edit Instructor</h1>

    <div class="top-buttons">
      <a href="{{ path('app_instructor_index') }}">Back to list</a>
    </div>

    {{ include('instructor/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('instructor/_delete_form.html.twig') }}
</div>
{% endblock %}
